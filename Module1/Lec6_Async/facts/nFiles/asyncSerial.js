const fs = require("fs");

let files = ["../f1.txt","../f2.txt","../f3.txt"]; // Array containing paths of  n files

// f1KaData => f2KaData => f3KaData

// loop?
// is this possible with doing loop as did in asyncparallel

//Not at all ,can't possible with any loop(for,while or foreach)
console.log("start");

// Try with recursive solution
//will do after doing Recursion in DS


console.log("end");